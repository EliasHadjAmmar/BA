

# `/external/` and `/input/` subdirectories are ignored as they should contain symbolic links, which are user-specific
*external/
*input/

# NOTE: in the GSlab template, they make an exception for paper_slides/input, where they create copies instead of symbolic links

# `/output_local/` subdirectories are ignored as they contain large outputs that will be stored outside of Github
*output_local/

# `/temp/` subdirectories are ignored as they are temporary by design
*temp/

# The following extensions indicated intermediate files generated by certain programs during compiling
*.aux
*.pdf
*.log
*.out
*.bbl
*.blg
*.synctex.gz
*.Rhistory
*.RData
.Rproj.user/
*.Rapp.history
*Rplots.pdf
*.html
*.ipynb_checkpoints/
*.DS_Store
*.sconsign.dblite

# Personal conda info
.conda_info

# Other
literature
drive
*.xlsx
