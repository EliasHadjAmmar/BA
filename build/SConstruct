
from SCons.Script import Command

clean_data = Command('output/clean_data.csv',   # target path (output file)
        'input/territories.csv',                # source path (input file)
        'Rscript code/clean_territories.R')     # command line string
                                           
